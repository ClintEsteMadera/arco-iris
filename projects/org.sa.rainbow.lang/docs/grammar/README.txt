Grammar files originally created in its separate directory, but ANTLR studio
requires the grammar and the generated token files to be in the same directory,
hence files have been moved to the parser package.

Old version of grammar files are stored under the directory ARCHIVE/.
